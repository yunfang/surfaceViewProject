<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/iv_mount_bg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:src="@drawable/general_mount_bg" />

    <ImageView
        android:id="@+id/iv_mount_icon"
        android:layout_width="115dip"
        android:layout_height="140dip"
        android:layout_alignLeft="@id/iv_mount_bg"
        android:layout_centerVertical="true"
        android:src="@drawable/car153" /> <!-- 143 × 202 -->

    <TextView
        android:id="@+id/tv_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/iv_mount_bg"
        android:layout_marginBottom="20dip"
        android:layout_marginLeft="20dip"
        android:layout_toRightOf="@id/iv_mount_icon"
        android:gravity="left"
        android:textColor="#ffffff"
        android:textSize="14sp"
        android:visibility="visible" />

</RelativeLayout>